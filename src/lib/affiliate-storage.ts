/**
 * @deprecated Use `@/lib/affiliate-api` — produtos agora ficam no SQLite do servidor.
 * Mantido só para compatibilidade de imports antigos de autenticação.
 */
export {
  ADMIN_PASSWORD,
  createProductId,
  isAdminAuthenticated,
  setAdminAuthenticated,
} from './affiliate-api';
